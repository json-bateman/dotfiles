export ZSH="$HOME/.oh-my-zsh"

ZSH_THEME="robbyrussell"

# zstyle ':omz:update' mode disabled  # disable automatic updates
zstyle ':omz:update' mode auto      # update automatically without asking
# zstyle ':omz:update' mode reminder  # just remind me to update when it's time

# Uncomment the following line to change how often to auto-update (in days).
zstyle ':omz:update' frequency 13

# Uncomment the following line if you want to disable marking untracked files
# under VCS as dirty. This makes repository status check for large repositories
# much, much faster.
DISABLE_UNTRACKED_FILES_DIRTY="true"

# Which plugins would you like to load?
# Standard plugins can be found in $ZSH/plugins/
# Custom plugins may be added to $ZSH_CUSTOM/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
# Add wisely, as too many plugins slow down shell startup.
plugins=(git autojump aws)

source $ZSH/oh-my-zsh.sh

# User configuration
# export MANPATH="/usr/local/man:$MANPATH"
export LANG=en_US.UTF-8

# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
# PATH and Environment Variables
# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
export EDITOR=nvim
export TERM=tmux-256color
export PATH="/Users/jacktrusler/dotfiles/scripts/:$PATH"

# ~~~~~~
# source 
# ~~~~~~
source /opt/homebrew/share/zsh-autosuggestions/zsh-autosuggestions.zsh
source /opt/homebrew/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh


# Vim mode for terminal
bindkey -v
export KEYTIMEOUT=1

# ~~~~~~~
# aliases 
# ~~~~~~~
alias tks='tmux kill-session'
alias tms='tmux-sessionizer'
alias f='cd$(fd --type directory | fzf)' # Jump into directory selected by fzf
alias lg='lazygit'

# ~~~~~~~~~~~~~
# history setup
# ~~~~~~~~~~~~~
HISTFILE=$HOME/.zhistory
SAVEHIST=1000
HISTSIZE=999
HIST_STAMPS="yyyy/mm/dd"
setopt share_history  # Updates history in multiple zsh windos
setopt hist_expire_dups_first # Removes duplicates
setopt hist_ignore_dups # if you do ls, then ls again, only record the second
setopt hist_verify # doesn't execute !! right away, shows the command first

# ~~~~~~~
# misc 
# ~~~~~~~
eval "$(~/.local/bin/mise activate zsh)"

